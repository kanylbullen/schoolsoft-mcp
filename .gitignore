# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.eggs/

# Virtual envs
.venv/
venv/
env/
ENV/

# Tooling
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# Captured HTML fixtures that may contain personal data
tests/fixtures/private/
*.local.html

# Endpoint discovery + probed JSON samples — contain real names, grades,
# messages, school slug, and auth-only paths. Strictly local.
discovery/

# Local secrets-manager configs (per-user, may include workspace IDs).
.phase.json
uv.lock
